# png-parser

A command-line utility to display the structure of a PNG file.

Requires .NET 10.0 runtime.

Usage: `png-parser -i <PNG file> [--print-text] [--print-time]` \
`--print-text`: print contents of the tEXt chunk (text data) \
`--print-time`: print contents of the tIME chunk (modification time)